<div class="category-index">
  All Categories
  <section>
    <% @categories.each do |category| %>
    <%= category.name %>
    <%= link_to "Delete Category", { action: :destroy, id: category.id }, method: :delete, data: "Please confirm delete" %>
    <% end %>
  </section>
</div>
